#!/usr/bin/env bash
# Blocks a commit when the committed bundles are stale (a module was edited but
# ./build.sh wasn't re-run). Enable with:
#   git config core.hooksPath site-scripts/.githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)/site-scripts"
./build.sh --check
